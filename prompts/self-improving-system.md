Set up the Knowledge Base layer for a self improving system in this directory.

1. First, scan this directory. If existing files or folders are here, propose a restructure plan -- what stays, what moves into raw/ what gets renamed -- and wait for my sign-off before touching anything. If empty, skip to step  2.

2. Create raw/ for my original unaltered assets. Never edit or reorganize anything inside it.

3. Create wiki/ as an AI-written table of contents that references files in raw/. AI-written only, never edited by hand.

4. Write a CLAUDE.md under 60  lines pinning these rules so every future session honors them.

End with a summary  of what you created and the rules you wrote into CLAUDE.md.


